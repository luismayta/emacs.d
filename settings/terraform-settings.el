;--------------------------------;
;;; Terraform Settings         ;;;
;--------------------------------;

;; Install Packages
(bundle 'terraform-mode nil)
(require 'terraform-mode)

(terraform-mode)

(provide 'terraform-settings)
