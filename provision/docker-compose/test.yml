# Docker Compose
# https://docs.docker.com/compose/
#
# Usage: test up

version: "3"
services:
  app:
    image: emacs.d-app-test
    environment: &environment
      - STAGE=testing
    build:
      args:
        stage: test
    volumes:
    - ./:/home/.emacs.d
    env_file:
      - .env
